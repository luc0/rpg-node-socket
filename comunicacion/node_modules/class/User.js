//------------------------------------------------------------------
// User
//------------------------------------------------------------------

exports.User = function( params ){

	this.username = params.username || '';

	// Conexion a la que pertenece el User
	this.socket = params.socket || null;

	/*
	this.generateId = function(){
		console.log('generando id...')
		var id;
		do{
			id = this.name + parseInt( Math.random() * 10000 );
		}while( id in world.createdObjects );
		return id;
	}
	*/
	this.socket = function( params ){
		return USERS.users[ params.userId ].socket;
	}
}

console.log('Clase User -> OK');
