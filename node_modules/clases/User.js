// Modelo User
global.User = function( options ){

	var defaults = {
		nombre:'',
		posicion:{'x':0 , 'y':0}
	}

	(function(self){
		for( prop in defaults ){
			self[prop] = options[prop] || defaults[prop] || null;
		}
	})(this)

	console.log(this);

}
