console.log('CLASES CARGADAS -> OK');

/*var classExtends = require('./classExtends.js');
var Scenario = require('./Scenario.js');
var Graphics = require('./Graphics.js');
var Personajes = require('./Personajes.js');*/

//------------------------------------------------------------------------------------------------
// USER
//------------------------------------------------------------------------------------------------

global.User = function(){
	
	this.nombre = '';
	this.pos = {x:0, y:0};
	//this.color = 'rgb(0,0,0)';

	// Asignamos posicion al AZAR.
	this.posicionar = function(){
		var xt = parseInt(Math.random()*10)+1;
		var yt = parseInt(Math.random()*10)+1;
		this.pos.x = xt * 15;
		this.pos.y = yt * 15;
	}

	// Asignamos un color al AZAR
	/*this.colorear = function(){
		var cr = parseInt(Math.random()*256);
		var cg = parseInt(Math.random()*256);
		var cb = parseInt(Math.random()*256);
		this.color = 'rgb(' + cr + ',' + cg + ',' + cb + ')';
	}*/


}